# Base URL of the deployed API.
# Defaults to http://localhost:3001 (local dev). Set this to the production
# worker URL when building for deployment, e.g.:
# PUBLIC_API_URL=https://kaizenlife-api.warid.web.id